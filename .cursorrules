# Cursor 项目规则

## 语言与回复

- **始终使用中文回复**。除非用户明确要求使用英文，否则所有解释、说明、代码注释和对话均使用简体中文。

## 工作区路径规则

**重要：所有文件操作必须在当前工作区内进行**

当前工作区路径：`D:\桌面\AI开发\代码\cursor\项目1`

⚠️ **注意**：此路径包含中文字符，在使用命令行工具时可能遇到编码问题。建议直接使用 Cursor 的文件操作工具。

### 规则说明

1. **文件创建位置**
   - 所有新建文件必须在当前工作区路径内
   - 禁止在 C:\Users\Administrator\ 或其他系统路径创建项目文件
   - 使用相对路径或确保使用正确的工作区绝对路径
   - 优先使用 Cursor 的 Write/StrReplace 工具，避免直接使用 PowerShell 操作中文路径

2. **路径验证**
   - 在创建文件前，验证目标路径是否在工作区内
   - 使用 Cursor 提供的工作区上下文信息
   - 工作区路径：`D:\桌面\AI开发\代码\cursor\项目1`

3. **文件操作最佳实践**
   - 优先使用工具提供的当前工作区路径
   - 创建文件前检查父目录是否存在
   - 所有项目相关文件必须在工作区目录树内

4. **禁止的操作**
   - ❌ 在用户主目录下创建项目文件
   - ❌ 在系统临时目录创建项目文件
   - ❌ 假设或猜测项目路径
   - ✅ 始终使用工作区提供的路径信息

---

*本规则确保所有生成的代码和文件都在正确的工作区位置，避免文件散落在不同目录。*
